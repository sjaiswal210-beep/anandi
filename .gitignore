# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
.next/
out/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/

# Turbo
.turbo/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
docker-compose.override.yml

# Prisma
prisma/migrations/*.sql

# Uploads
uploads/
!uploads/tts/
tmp/

# Certificates
*.pem
*.key
*.crt

# HyperFrames render output and scratch files.
# Rendered MP4s are tens of MB each; they are rebuilt from the composition, so
# they do not belong in the repo (which is public).
anandi-park-promo/renders/
anandi-park-promo/snapshots/
anandi-park-promo/.media/
anandi-park-promo/.hyperframes/
anandi-park-promo/check-out.txt

# Handoff archives (may contain real .env values — never commit)
anandi-park-handoff*.zip
/../anandi-park-handoff*.zip

# TypeScript incremental build info
*.tsbuildinfo
